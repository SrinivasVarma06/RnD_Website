# ─── R&D Data API Server ─────────────────────────────
# Copy this file to .env and fill in values

# Port the API server listens on (default: 5000)
PORT=5000

# How often to refresh data from Google Sheets (minutes)
REFRESH_MINUTES=10

# Admin password — CHANGE THIS in production!
# Used for admin login (hashed with bcrypt, never stored as plaintext)
ADMIN_PASSWORD=ChangeThisPassword!
